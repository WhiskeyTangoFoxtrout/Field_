import * from _Barnacle_buster, curses, * from Rails.py


class egress:

    def __init__(self):
        name == 'Operation System'

        while True:#feel like this should be wit the curses module
            indown(text = input('CmdPmt>'))
            print(indown(text))


    def indown(self):#mainwindow and bootup
        scr = curses.init()#this is the whole screen
        scr.refresh()
        scr.noecho()
        scr.nocbreak()
        scr.def_shell_mode()
        scr(movEprint("""This is a starter working on the whole kernal thing be on the look out for more updates this also is
        the actual testing kernal so the fact you found this says a lot. Bless'd...also, where there is a compiler; updates, mainly "Dot E-X-E Files".
        """))
        if scr.beep() < 5:#ima have to check this
            for scr.beep() in range(0,4):
                curses.beep()
            for scr.flash() in range(0,1):
                curses.flash()
        scr.refresh()
        #scr.insertln() this gone act like a history
        new
        
        scr.doupdate()
        
        
        
        
        
